<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Radio Taxis Libres S.A.</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
          integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>
<div class="page-header">
    <h1 style="text-align: center">
        Radio Taxis Libres S.A.
        <small>Conjuntos</small>
    </h1>
</div>
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <h2 class="controller_name">Taxistas Asociados: {{ taxistas|length }}</h2>
            <button id="agregar" type="button" class="btn btn-success">Agregar</button>
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Placa</th>
                </tr>
                </thead>
                <tbody>
                {% for taxis in taxistas %}
                    <tr>
                        <td>{{ taxis._id }}</td>
                        <td>{{ taxis.nombre }}</td>
                        <td>{{ taxis.placa }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <div>
        <div id="formulario" class="form-group">
            <label for="usr">Nombre:</label>
            <input type="text" class="form-control" id="usr">
            <label for="pwd">Placa:</label>
            <input type="password" class="form-control" id="pwd">
            <input type="submit" class="btn btn-default" value="Enviar" id="enviar">
        </div>
    </div>
    <div id="footer" class="alert alert-info">&copy; Radio Taxis Libes S.A.</div>
    {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.2.1.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <script>
            $(document).ready()
            {
                $('#formulario').hide();
                $('#agregar').on('click', function () {
                    $('#formulario').fadeIn();
                })
                $('#enviar').on('click', function (evt) {
                    evt.preventDefault();
                    console.log('Funciona');
                    $.get('ajax.php').done(function(res) {
                        console.log(res);
                    });
                })
                console.log('Listo');
            }
        </script>
    {% endblock %}
</div>
</body>
</html>